# nRF_EPD
1. 盒马 价签 nRF52811 系列，2.13，2.9， 4.2，7.5系列价签 开发
2. 基于tsl0922 工程开发 https://github.com/tsl0922/EPD-nRF5

# 版本说明
 ## V 0.0.1
  同步 tsl0922 未做任何修改

# BLE 协议兼容说明
- 更完整的命令表、通知格式和兼容说明见 [docs/ble-protocol.md](docs/ble-protocol.md)。
- 固件 `0x21` 起新增 `EPD_CMD_SYNC_TIME_SILENT (0x23)`，用于“连接后静默校时”，只更新时间，不立即触发屏幕刷新。
- 旧命令 `EPD_CMD_SET_TIME (0x20)` 继续保持兼容语义：无论是否携带 mode 参数，都会立即触发刷新，避免影响历史网页端或第三方客户端。
- 网页端连接后仅在检测到固件版本 `>= 0x21` 时才会自动发送 `0x23`；旧固件会跳过自动校时，避免连接瞬间意外刷屏。
- MCU 在收到 `EPD_CMD_INIT (0x01)` 后会通过通知额外上报 `v=<mV>`，网页端据此显示当前电池电压；仅建立 BLE 连接但未发送 `INIT` 的客户端不会收到该电压上报。

# 低电量行为说明
- 低电量阈值为 `<= 2800mV`：MCU 停止正常自动刷新，屏幕显示电池样式和当前电压。
- 临界关机阈值为 `< 2700mV`：MCU 会先尝试刷新出 `power off` 提示，再进入 `SYSOFF` 关机。
- 低电量页激活后，固件每 `10` 秒轮询一次电压，用于检测“继续跌压关机”或“恢复到 >2800mV 后退出低电量模式”。


